---
title: "GET /api/sections"
description: "Retrieve all sections for subjects belonging to the authenticated user."
---

## Overview

Returns every section whose parent subject is owned by the authenticated user. Each section includes its associated days, subject, and professor (if assigned). Sections belonging to subjects owned by other users are never included.

<Note>
  Day IDs use a fixed reference: 1 = Monday, 2 = Tuesday, 3 = Wednesday, 4 = Thursday, 5 = Friday, 6 = Saturday, 7 = Sunday.
</Note>

## Request

### HTTP method and path

`GET /api/sections`

### Authentication

Bearer token required.

```
Authorization: Bearer <accessToken>
```

### Request body

None.

## Response

HTTP `200 OK`

An array of section objects. Returns an empty array `[]` when the user has no sections.

<ResponseField name="[]" type="object[]" required>
  <Expandable title="section properties">
    <ResponseField name="id" type="string" required>
      UUID that uniquely identifies the section.
    </ResponseField>
    <ResponseField name="startTime" type="string" required>
      Start time as an ISO 8601 timestamp.
    </ResponseField>
    <ResponseField name="endTime" type="string" required>
      End time as an ISO 8601 timestamp.
    </ResponseField>
    <ResponseField name="subjectId" type="string" required>
      UUID of the parent subject.
    </ResponseField>
    <ResponseField name="professorId" type="string">
      UUID of the assigned professor, or `null` if unassigned.
    </ResponseField>
    <ResponseField name="sectionDays" type="object[]" required>
      <Expandable title="sectionDay properties">
        <ResponseField name="id" type="string" required>
          UUID of the section-day join record.
        </ResponseField>
        <ResponseField name="sectionId" type="string" required>
          UUID of the parent section.
        </ResponseField>
        <ResponseField name="dayId" type="number" required>
          Day ID (1–7).
        </ResponseField>
        <ResponseField name="day" type="object" required>
          <Expandable title="day properties">
            <ResponseField name="id" type="number" required>
              Numeric day ID.
            </ResponseField>
            <ResponseField name="name" type="string" required>
              Lowercase day name (e.g., `"tuesday"`).
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Example

<CodeGroup>

```bash curl
curl --request GET \
  --url https://api.scheduly.app/api/sections \
  --header 'Authorization: Bearer <accessToken>'
```

```json Response
[
  {
    "id": "d4e5f6a7-b8c9-0d1e-2f3a-4b5c6d7e8f9a",
    "startTime": "1970-01-01T08:00:00.000Z",
    "endTime": "1970-01-01T10:00:00.000Z",
    "subjectId": "b1c2d3e4-5f6a-7b8c-9d0e-1f2a3b4c5d6e",
    "professorId": "e5f6a7b8-9c0d-1e2f-3a4b-5c6d7e8f9a0b",
    "sectionDays": [
      {
        "id": "a1b2c3d4-e5f6-7a8b-9c0d-e1f2a3b4c5d6",
        "sectionId": "d4e5f6a7-b8c9-0d1e-2f3a-4b5c6d7e8f9a",
        "dayId": 1,
        "day": { "id": 1, "name": "monday" }
      },
      {
        "id": "b2c3d4e5-f6a7-8b9c-0d1e-f2a3b4c5d6e7",
        "sectionId": "d4e5f6a7-b8c9-0d1e-2f3a-4b5c6d7e8f9a",
        "dayId": 3,
        "day": { "id": 3, "name": "wednesday" }
      }
    ]
  }
]
```

</CodeGroup>

## Error responses

| Status | Description |
| ------ | ----------- |
| `401 Unauthorized` | The access token is missing, malformed, or expired. Obtain a fresh token from `/api/auth/refresh`. |
